# google-adk needs google-cloud-aiplatform>=1.87; do not install PyPI "vertexai"
# (it pins older aiplatform and conflicts). The `vertexai` module comes from aiplatform.
google-adk>=0.5.0
google-cloud-aiplatform>=1.87.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
httpx>=0.27.0
structlog>=24.1.0
asyncpg>=0.29.0
redis>=5.0.4
python-dotenv>=1.0.1
